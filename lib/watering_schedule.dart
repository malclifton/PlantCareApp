// watering_schedule.dart
import 'package:flutter/material.dart';

class WateringSchedulePage extends StatelessWidget {
  const WateringSchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Watering Schedule')),
      body: const Center(
        child: Text('Watering Schedule Page'),
      ),
    );
  }
}
